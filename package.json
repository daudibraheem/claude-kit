{
  "name": "claude-scout-workspace",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @claude-scout/cli dev"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
